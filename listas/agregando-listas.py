# Agregando usando ZIP
# armazenar mais informação em variáveis
# manter a sequencia de uma variável

cores = ['amarelo', 'verde', 'azul', 'vermelho']
valores = [10, 20, 30, 40]

x = list(zip(cores, valores))

print(x)
